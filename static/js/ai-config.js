/* Novel Studio — AI config page (stub, to be built in Task 14) */
window.AIConfig = window.AIConfig || {
    config: {},
    async load() { console.log('[AIConfig] load'); },
    render() { console.log('[AIConfig] render'); },
    async save() { console.log('[AIConfig] save'); },
};
