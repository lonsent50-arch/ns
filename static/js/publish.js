/* Novel Studio — Publish module (stub, to be built in Task 16) */
window.Publish = window.Publish || {
    async load(projectId) { console.log('[Publish] load', projectId); },
    render() { console.log('[Publish] render'); },
    async export(format) { console.log('[Publish] export', format); },
};
